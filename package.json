{
  "name": "goaloracle",
  "version": "1.0.0",
  "description": "World Cup 2026 AI Prediction Market on X Layer",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.js --network xlayer_testnet",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network xlayer_mainnet",
    "seed": "hardhat run scripts/seedMatches.js --network xlayer_testnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.28.6"
  },
  "dependencies": {
    "dotenv": "^16.6.1"
  }
}
